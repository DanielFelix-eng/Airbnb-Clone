# TODO

- [ ] Update backend password reset email link to use the reset password frontend domain instead of localhost.
- [ ] Ensure reset link preserves the token query param so `frontend/src/pages/resetPassword.jsx` can read it.
- [ ] (Optional) Update any other reset-link occurrences (templates/controllers) to keep URLs consistent.
- [ ] Run a quick backend/frontend check (lint/test/build) if available.

